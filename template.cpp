#include <bits/stdc++.h>
#include <algorithm>
using namespace std;
typedef long long ll;
#define all(a) a.begin(), a.end()
#define print(arr)     \
    for (auto x : arr) \
        cout << x << " ";

string bts(int x) { bitset<32> bset(x); return bset.to_string(); }
void solve()
{
    int n,m,w,x,y,z;
    cin>>n>>m;
    
};
signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    // solve();
    int tc;
    cin >> tc;
    while (tc--)
    {
        solve();
    }
}